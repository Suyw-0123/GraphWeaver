
services:
  # Database
  postgres:
    image: postgres:15-alpine
    container_name: graphweaver-postgres
    environment:
      POSTGRES_USER: graphweaver
      POSTGRES_PASSWORD: graphweaver123
      POSTGRES_DB: graphweaver
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - graphweaver-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U graphweaver -d graphweaver"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Backend API
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: graphweaver-backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - GIN_MODE=release
      - PORT=8080
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=graphweaver
      - DB_PASSWORD=graphweaver123
      - DB_NAME=graphweaver
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL_NAME=${GEMINI_MODEL_NAME:-gemini-pro}
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6334
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/app/uploads
    networks:
      - graphweaver-net
    # Run migrations before starting the server
    command: sh -c "./migrate -path migrations/postgres -database 'postgres://graphweaver:graphweaver123@postgres:5432/graphweaver?sslmode=disable' up && ./server"

  # Frontend Web App
  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: graphweaver-frontend
    depends_on:
      - backend
    ports:
      - "80:80"
    networks:
      - graphweaver-net

  # Future Services (Placeholder)
  # neo4j:
  #   image: neo4j:5.15-community
  #   ...
  # Qdrant Vector Database
  qdrant:
    image: qdrant/qdrant:latest
    container_name: graphweaver-qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    networks:
      - graphweaver-net
    healthcheck:
      test: ["CMD-SHELL", "bash -c ':> /dev/tcp/127.0.0.1/6333' || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5

networks:
  graphweaver-net:
    driver: bridge

volumes:
  postgres_data:
  qdrant_storage:
